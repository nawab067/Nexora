'use client';

import AuthPage from '@/components/authdemo';

export default function Auth() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-white">
      <div className="w-full max-w-md">
        <AuthPage />
      </div>
    </main>
  );
}